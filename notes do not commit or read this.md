execution of command should be via a subagent with a clear/cleaned context. caller can interact as needed to manage the issue and verify work

Transcoder needs to handle removing non-english audio streams

Transcoder needs to handle removing non-english subtitle streams

Transcoder needs to handle 7.1/5.1/etc to 2/2.1 conversion

Track transcoding metrics for runtime, fps, source and destination codec types, hwaccel, etc

consistent logging

readme

enable `wsl` linter

e2e tests

specify specific path to device

black bar cropping (?)

embed artwork

set audio default track to english

Blacklist release on repeated workflow failures

makefile target for updates, that makes the docker compose tags match the go mod versions

consider combining movie + show workflows, and pass the type in with event

Agent's done seem to be willing to run unit tests unless I explicitly tell them that they can

Should watcher/scan workflow live under /workflows?
