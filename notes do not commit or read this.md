readme - document all env var and config format, as well as all metrics (though this could be in a separate file)

e2e tests

black bar cropping (?)

Blacklist release on repeated workflow failures

makefile target for updates, that makes the docker compose tags match the go mod versions

Agent's dont seem to be willing to run unit tests unless I explicitly tell them that they can

Should watcher/scan workflow live under /workflows?

merge media/shared packages

transcoder human name in metrics

**Files need to be in the directory reported by download client**

metadata about media added to container?

make source file deletion optional (for torrents)

remove empty source directories

standardize env var names, rethink config vs env var for each

add a benchmark using a full movie
